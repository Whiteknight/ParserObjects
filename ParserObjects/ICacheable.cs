namespace ParserObjects
{
    /// <summary>
    /// A flag type to indicate that certain objects in the system can be used as a cache key.
    /// </summary>
    public interface ICacheable
    {
    }
}
